<div class="w-25 mt-3">
  <h1 class='title'>All your relationships (<%= relations.size %>)</h1>
  <%= link_to raw('<i class="fa-solid fa-bullseye""></i> Add new'), new_relation_path, class: 'mt-5 btn btn-grey' %>

  <ul class='mt-3 container'>
    <% relations.each do |relation| %>
      <li class='card-text row'>
        <p class='col-6'><%= relation.relation_type %> <%= "(#{relation.contacts.count} contacts)"%></p>
        <%= link_to '🎨', edit_relation_path(relation), class:'link col-3' %>
        <%= link_to '💣', relation_path(relation), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to permanently delete #{relation.relation_type} ? It will also be deleted from the contact records." }, class:'link col-3'  %>
      </li>
    <% end %>
  </ul>
</div>
