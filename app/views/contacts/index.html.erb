<h1>All your contacts (<%= @contacts.count %>)</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <%= link_to contact_path(contact) do %>
        <%= contact.first_name  %> <%= contact.last_name %>
      <% end %>
    </li>
  <% end %>
</ul>
