# this helper allows us to have a js stimulus clipboard target for simple_format zones
module ApplicationHelper
  # def formatted_infos_with_clipboard(infos)
  #   tag.p(raw(sanitize(infos)), class: 'front-input', data: { clipboard_target: 'source' })
  # end
end
